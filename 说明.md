# Cutly-Ai辅助影视本地化工作台技术与迁移说明

## 1. 架构概览

当前服务器地址为 `172.16.1.24`，浏览器访问 `http://172.16.1.24:8000/d`。前端是 `static/index.html` 单文件 SPA；后端是 `workstation.py`（FastAPI、API 路由、任务状态机、文件和项目管理）；`pipeline.py` 是本地化引擎。重任务在线程中执行，前端轮询任务进度。

任务主流程：

```text
上传 → 音频分离 → Whisper 识别字幕 → 人工精修 → LLM 翻译 → TTS 配音
     → Demucs 去人声 → 时间轴编辑 → FFmpeg 视频合成 → 下载
```

项目库批量操作按项目逐个串行执行，不并发轰击模型或外部服务。

视频合成成功后会顺序调用当前配置的 LLM：先基于中文字幕生成中文剧情钩子和 #标签，再翻译为英语和当前目标语言；结果保存到项目的 `promo_copy`，可从主页面或项目库的视频下载按钮旁复制。

## 2. 目录与外部依赖

| 路径 | 用途 |
|---|---|
| `/www/wwwroot/aidj/drama_localizer` | 生产运行目录：源码副本、`.env`、Python venv、`output/jobs`、字体、运行日志 |
| `/www/wwwroot/aidj/drama_localizer_ym` | 源码管理和迁移目录；同时保存 `models/` 模型缓存与本文档 |
| `/www/wwwroot/aidj/drama_localizer/output/jobs` | 任务、项目清单、媒体和派生文件 |
| `/home/juzer/.fonts` | fontconfig 字体副本，供 FFmpeg/libass 合成字幕使用 |
| `/etc/systemd/system/drama_localizer.service` | systemd 开机启动、自动重启和日志重定向 |
| `/usr/bin/ffmpeg`、`/usr/bin/ffprobe` | 音视频转码、混音、字幕和最终合成 |

实际模型缓存：`/www/wwwroot/aidj/drama_localizer_ym/models/{huggingface,torch,cache}`。源码内置字体位于 `/www/wwwroot/aidj/drama_localizer_ym/fonts/`。

`/home/juzer/.cache` 不是迁移必需目录：fontconfig、Hugging Face 旧缓存和其它用户缓存会在需要时自动重建。当前服务通过 `run.sh` 把模型缓存固定指向 `drama_localizer_ym/models/`，因此迁移时不要依赖或复制 `/home/juzer/.cache`。

## 3. 模型和联网服务

- `faster-whisper`：本地 Whisper，当前 `WHISPER_MODEL=large-v3-turbo`，用于中文语音识别和时间戳字幕。
- 阿里云百炼 OpenAI 兼容接口：当前 `LLM_MODEL=deepseek-v4-flash`，用于字幕翻译；每次优先三句，失败时逐句重试。
- 微软 Edge TTS：当前 `TTS_PROVIDER=edge`，用于目标语言逐句配音；声线由语言和女声/男声/旁白标识选择。
- Demucs `htdemucs`：本地运行，用于去除人声、保留伴奏。
- FFmpeg：本地运行，用于转码、音频对齐、混音、图片/字幕叠加和视频合成。

运行时主动联网的服务只有翻译接口和 Edge TTS。Whisper/Demucs 首次缺少模型时可能从 Hugging Face 下载；模型完整缓存后本地运行。Google Cloud TTS 是备用配置，当前未启用。

## 4. 首次部署（全新 Ubuntu 服务器）

### 4.1 服务器要求

- 推荐 Ubuntu 24.04/26.04 LTS、x86_64、至少 8 GB 内存、4 GB Swap、50 GB 磁盘。
- 使用对应 Ubuntu 自带的 `python3`；当前生产环境已在 Python 3.14 上验证。
- 服务器需要能访问配置中的 LLM 接口和 Edge TTS；模型已经随迁移目录复制时，Whisper/Demucs 不需要重新下载。

### 4.2 安装系统依赖并创建用户

使用具有 sudo 权限的账号执行：

```bash
sudo apt update
sudo apt install -y python3 python3-venv python3-pip ffmpeg curl fontconfig \
  ca-certificates build-essential libsndfile1 rsync

# 已经存在 juzer 用户时不会重复创建
id juzer >/dev/null 2>&1 || sudo useradd -m -s /bin/bash juzer
sudo install -d -o juzer -g juzer /www/wwwroot/aidj/drama_localizer
sudo install -d -o juzer -g juzer /www/wwwroot/aidj/drama_localizer_ym
```

确认 FFmpeg 同时包含字幕滤镜；输出必须能看到 `subtitles`：

```bash
ffmpeg -version
ffmpeg -hide_banner -filters | grep subtitles
python3 --version
```

如果服务器没有 Swap，创建 4 GB Swap；已经有 Swap 时跳过：

```bash
swapon --show
if ! swapon --show=NAME --noheadings | grep -q .; then
  sudo fallocate -l 4G /swap.img
  sudo chmod 600 /swap.img
  sudo mkswap /swap.img
  sudo swapon /swap.img
  echo '/swap.img none swap sw 0 0' | sudo tee -a /etc/fstab
fi
```

### 4.3 复制源码、模型和项目数据

把完整迁移目录放到：

```text
/www/wwwroot/aidj/drama_localizer_ym
```

该目录必须保留 `models/` 和 `fonts/`。`fonts/` 内是可直接部署的字体文件，不依赖下次联网下载。然后把运行所需源码复制到生产目录：

```bash
sudo rsync -a --delete \
  --exclude models --exclude TECHNICAL.md --exclude .git \
  --exclude .env --exclude venv --exclude output --exclude backups \
  --exclude workstation.log \
  /www/wwwroot/aidj/drama_localizer_ym/ /www/wwwroot/aidj/drama_localizer/
sudo chown -R juzer:juzer /www/wwwroot/aidj/drama_localizer /www/wwwroot/aidj/drama_localizer_ym
sudo chmod +x /www/wwwroot/aidj/drama_localizer/run.sh
```

迁移已有项目时，旧服务器的以下内容也必须复制到新服务器相同位置：

```text
/www/wwwroot/aidj/drama_localizer/.env
/www/wwwroot/aidj/drama_localizer/output/jobs/
```

不要用空目录覆盖已有的 `output/jobs`。复制数据前应先停止旧服务器服务，避免 `jobs.json` 与媒体文件不一致。

### 4.4 创建 Python 虚拟环境

```bash
sudo -u juzer python3 -m venv /www/wwwroot/aidj/drama_localizer/venv
sudo -u juzer /www/wwwroot/aidj/drama_localizer/venv/bin/python -m pip install --upgrade pip wheel
sudo -u juzer /www/wwwroot/aidj/drama_localizer/venv/bin/pip install \
  -r /www/wwwroot/aidj/drama_localizer/requirements.txt
```

`requirements.txt` 已包含部署脚本所需的 `paramiko`，因此迁移后可直接运行 `deploy_d.py`。

验证核心模块：

```bash
cd /www/wwwroot/aidj/drama_localizer
sudo -u juzer /www/wwwroot/aidj/drama_localizer/venv/bin/python -c \
  'import workstation, pipeline; print(workstation.APP_VER, pipeline.WHISPER_MODEL)'
```

### 4.5 配置 `.env`

新部署可从样例创建；迁移则直接安全复制原服务器的 `.env`：

```bash
sudo -u juzer cp /www/wwwroot/aidj/drama_localizer/.env.example \
  /www/wwwroot/aidj/drama_localizer/.env
sudo chmod 600 /www/wwwroot/aidj/drama_localizer/.env
sudo chown juzer:juzer /www/wwwroot/aidj/drama_localizer/.env
sudo nano /www/wwwroot/aidj/drama_localizer/.env
```

至少确认以下配置存在且正确：

```dotenv
WORKSTATION_API_KEY=请设置工作台访问密钥
WHISPER_MODEL=large-v3-turbo
LLM_API_KEY=请设置LLM密钥
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v4-flash
TTS_PROVIDER=edge
SRC_LANG=zh
TGT_LANG=km
TARGET_LANG=km
```

`TGT_LANG` 是底层流水线默认目标语言，`TARGET_LANG` 是工作台新项目默认目标语言；两项通常设置成相同值。前端仍可针对单个项目切换目标语言。

不要把真实 `.env` 提交到 Git 或放进公开压缩包。

### 4.6 安装字幕字体

字体文件已经随源码保存在 `/www/wwwroot/aidj/drama_localizer_ym/fonts/`。前面的 `rsync` 会把它们复制到生产目录；再注册到 fontconfig：

```bash
sudo install -d -o juzer -g juzer /home/juzer/.fonts
sudo cp -f /www/wwwroot/aidj/drama_localizer/fonts/*.woff2 /home/juzer/.fonts/
sudo chown -R juzer:juzer /home/juzer/.fonts
sudo -u juzer fc-cache -f
sudo -u juzer fc-match 'Noto Sans Khmer'
```

只有源码字体目录意外缺失、且服务器可以联网时，才需要执行备用下载脚本：

```bash
sudo -u juzer bash /www/wwwroot/aidj/drama_localizer/fetch_fonts.sh
```

日常执行 `deploy_d.py` 时也会自动上传源码中的 `fonts/`，复制到 `/home/juzer/.fonts` 并刷新 `fc-cache`，不需要手工重复安装。

### 4.7 安装 systemd 服务并设置开机启动

创建 `/etc/systemd/system/drama_localizer.service`：

```bash
sudo tee /etc/systemd/system/drama_localizer.service >/dev/null <<'EOF'
[Unit]
Description=Cutly Drama Localizer (uvicorn workstation:app)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=juzer
Group=juzer
WorkingDirectory=/www/wwwroot/aidj/drama_localizer
ExecStart=/www/wwwroot/aidj/drama_localizer/run.sh
Restart=always
RestartSec=3
Environment=PYTHONUNBUFFERED=1
EnvironmentFile=-/www/wwwroot/aidj/drama_localizer/.env
UMask=0027
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=full
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
LockPersonality=true
KillMode=mixed
TimeoutStopSec=30
StandardOutput=append:/www/wwwroot/aidj/drama_localizer/workstation.log
StandardError=append:/www/wwwroot/aidj/drama_localizer/workstation.log

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now drama_localizer
```

`enable --now` 同时完成“立即启动”和“以后随系统开机启动”。验证：

```bash
systemctl is-enabled drama_localizer
systemctl is-active drama_localizer
sudo systemctl status drama_localizer --no-pager
curl -fsS http://127.0.0.1:8000/d/api/version
```

预期前两条分别返回 `enabled` 和 `active`。如果其他电脑无法访问，检查云安全组或防火墙是否允许 TCP 8000；仅在确实启用 UFW 时执行：

```bash
sudo ufw allow 8000/tcp
sudo ufw status
```

### 4.8 首次启动验收与故障排查

```bash
curl -I http://127.0.0.1:8000/d/
curl -fsS http://127.0.0.1:8000/d/api/version
tail -n 100 /www/wwwroot/aidj/drama_localizer/workstation.log
journalctl -u drama_localizer -n 100 --no-pager
```

浏览器访问 `http://<服务器IP>:8000/d`，输入 `.env` 中的 `WORKSTATION_API_KEY`。至少完成一次上传、字幕识别、字幕翻译、语音生成、去人声和视频合成测试。

### 4.9 搬迁打包注意事项

Hugging Face 模型缓存内部包含符号链接。优先使用 `rsync -a` 或 Linux `tar` 搬迁；不要用会丢失符号链接的普通 Windows 压缩工具重新打包模型目录。

旧服务器停止服务后，可生成完整迁移包：

```bash
sudo systemctl stop drama_localizer
sudo tar --numeric-owner -czpf /tmp/cutly-migration.tar.gz \
  /www/wwwroot/aidj/drama_localizer_ym \
  /www/wwwroot/aidj/drama_localizer/.env \
  /www/wwwroot/aidj/drama_localizer/output/jobs \
  /home/juzer/说明.md
sudo systemctl start drama_localizer
```

在新服务器解压时保留权限和符号链接：

```bash
sudo tar -xzpf cutly-migration.tar.gz -C /
sudo chown -R juzer:juzer /www/wwwroot/aidj/drama_localizer_ym \
  /www/wwwroot/aidj/drama_localizer/output/jobs /www/wwwroot/aidj/drama_localizer/.env
sudo chmod 600 /www/wwwroot/aidj/drama_localizer/.env
```

迁移包包含 API 密钥和项目媒体，必须按敏感文件保管，不要上传公共网盘或代码仓库。

### 4.10 小白迁移验收清单

依次检查，全部通过才算迁移完成：

1. `/www/wwwroot/aidj/drama_localizer_ym` 中存在源码、`models/`、`fonts/`。
2. `/www/wwwroot/aidj/drama_localizer/.env` 权限是 `600`，并包含工作台、LLM 和 TTS 配置。
3. `/www/wwwroot/aidj/drama_localizer/output/jobs` 已复制；旧项目需要保留时不能漏掉。
4. `ffmpeg -hide_banner -filters | grep subtitles` 能找到字幕滤镜。
5. `sudo -u juzer fc-match 'Noto Sans Khmer'` 能找到高棉字体。
6. `systemctl is-enabled drama_localizer` 返回 `enabled`。
7. `systemctl is-active drama_localizer` 返回 `active`。
8. `curl http://127.0.0.1:8000/d/api/version` 返回当前版本。
9. 另一台电脑可以打开 `http://<新服务器IP>:8000/d`。
10. 实测上传、识别、翻译、配音、去人声、带图片和字幕的视频合成。

Whisper 和 Demucs 已随模型目录迁移后可以离线运行；字幕翻译和 Edge TTS 仍然需要新服务器能够访问互联网及对应服务。

## 5. 日常开发与发布

只修改本地源码根目录。前端或后端实质修改时，同时提升 `static/index.html` 和 `workstation.py` 的 `APP_VER`，然后从源码目录执行：

```bash
export DRAMA_SSH_PASSWORD='服务器密码'
export DRAMA_HOST='172.16.1.24'
export WORKSTATION_API_KEY="$(tr -d '\\r\\n' < WORKSTATION_ACCESS_KEY.txt)"
python deploy_d.py
```

如果迁移后服务器 IP 改变，不要改死源码，可在执行部署前设置：

```bash
export DRAMA_HOST='新服务器IP'
export DRAMA_USER='juzer'
export DRAMA_REMOTE='/www/wwwroot/aidj/drama_localizer'
```

`DRAMA_HOST` 是部署工具的目标地址，不属于应用源码；迁移到其它地址时只改这个环境变量即可。

### 迁移到任意服务器

复制整个 `/www/wwwroot/aidj` 目录后，先安装系统依赖和 Python 依赖，再执行：

```bash
cd /www/wwwroot/aidj
sudo bash install.sh
```

`install.sh` 会根据当前目录自动生成 systemd 服务，设置运行目录、模型目录、日志路径、字体安装和开机启动，不依赖原服务器的 `/home/juzer` 路径。若新服务器运行用户不是目录所有者，可使用 `sudo CUTLY_USER=用户名 bash install.sh`。仅复制文件仍不能替代系统依赖安装，至少需要 `python3-venv`、`ffmpeg`、`fontconfig` 和 `libsndfile1`。

首次从一台新开发电脑连接服务器时，先手工执行一次 `ssh juzer@新服务器IP` 并确认主机指纹，否则部署脚本的 SSH 安全校验会拒绝陌生主机。`WORKSTATION_ACCESS_KEY.txt` 只放在自己的开发电脑上，内容应与生产 `.env` 的 `WORKSTATION_API_KEY` 一致，并设置为仅自己可读；不要把它提交到 Git 或放进公开迁移包。

部署脚本会上传源码、远端编译校验、创建回滚备份、重启服务并做健康检查。部署后浏览器执行 `Ctrl+Shift+R`。发布完成后同步源码到 `/www/wwwroot/aidj/drama_localizer_ym`；不要直接修改运行目录代替源码修改。

## 6. 数据、日志与备份

- 每个任务目录位于 `output/jobs/{jid}/`；项目保存后包含 `project.json` 和关联媒体。
- `jobs.json`、`projects.json` 是运行索引；删除项目会删除其任务目录和媒体。
- 全局日志为 `/www/wwwroot/aidj/drama_localizer/workstation.log`，达到 1MB 后在下一次应用日志写入前自动清空。
- 部署回滚备份位于 `/www/wwwroot/aidj/drama_localizer/backups/`；除非明确要求，不要自动删除。
- 迁移前停止服务并复制 `.env`、`output/jobs`、`drama_localizer_ym/models`；密钥单独安全传输。

## 7. 常用运维命令

```bash
sudo systemctl status drama_localizer
sudo systemctl restart drama_localizer
tail -f /www/wwwroot/aidj/drama_localizer/workstation.log
du -sh /www/wwwroot/aidj/drama_localizer/output/jobs
du -sh /www/wwwroot/aidj/drama_localizer_ym/models/*
```

## 8. 版本与回滚

以 `/d/api/version` 确认前后端版本一致。发布失败时停止服务，将最近一次备份中的文件恢复到运行目录，再重启并做健康检查。源码目录始终作为唯一修改入口。
