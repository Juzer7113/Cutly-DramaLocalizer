# v20260828013 全盘审计记录

## 已修复

- 任务 API、素材文件、API 文档增加访问密钥认证和 HttpOnly 会话 Cookie。
- 增加 CSP、`nosniff`、禁止嵌入、同源 Referrer 等安全响应头。
- 上传文件名净化、媒体类型白名单、保存路径约束和单文件大小限制。
- 前端动态内容进行 HTML/属性转义，后端限制轨道 ID、字幕长度和样式字段。
- ASS 颜色和字体输入净化，避免非法样式导致导出异常或注入。
- `jobs.json` 改为原子写入，保留最近有效备份，主文件损坏时自动尝试备份。
- 修复轨道 ASR/翻译/配音接口在持锁时再次加锁造成的死锁。
- 每个 Job 只允许一个后台操作；中止或过期线程不能覆盖新状态。
- 后台操作成功但未设置状态时自动恢复前态，避免永久停留 `running`。
- 上传大文件不再长时间占用全局任务锁。
- 修复版本徽标重复显示 `v`。
- 所有部署/诊断脚本移除明文 SSH 密码和 LLM Key，并启用 known_hosts 校验。
- 部署脚本改为暂存校验、部署前备份、环境变量注入和鉴权健康检查。
- systemd 增加权限收紧、私有临时目录、停止超时和安全 umask。
- 锁定生产依赖版本，增加 Git 忽略规则和自动化回归测试。

## 验证口径

- Python `compileall`、Shell `bash -n`、Node `--check`。
- 标准库 `unittest`：鉴权、上传路径、上传上限、原子持久化、任务互斥、
  中止后禁止陈旧回写、轨道 ID 校验、删除最后轨道后不复活。
- `pip-audit`：锁定依赖无已知漏洞。
- Bandit：业务代码不使用 `shell=True`；ffmpeg/ffprobe/demucs 均以参数数组调用。

## 运维动作

- 正式环境应使用 32 位以上随机 `WORKSTATION_API_KEY`；当前生产为测试密钥 `Aa123456`。
- SSH 登录密码已轮换；阿里云百炼 `LLM_API_KEY` 按要求保持现状不变。
- 原始 `drama_localizer_src_v20260828012.zip` 仅可作为受控旧快照保存，不得继续分发。
